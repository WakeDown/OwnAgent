@model OwnAgent.ViewModels.MarketServiceConditionViewModel
@{
    Layout = null;
}

@if (!String.IsNullOrEmpty(Model.Name))
{
    <div>@Model.Name</div>
    <div class="text-sm text-muted">@Model.Comment</div>
}
else
{
    <span>Не определено</span>
}
